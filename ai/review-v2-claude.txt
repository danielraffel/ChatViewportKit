Claude review of `ai/review-v2.txt`

Session:
- `18cb8448-525a-41ef-a5cc-00ee4283f531`

Verdict:
- design-level yes
- implementation-level conditional

Claude's requirement check:
- `LazyVStack`: met
- bottom anchor from start: met
- scroll commands for bottom, top, and ID: met
- no inverse scrolling: met
- smooth bottom animation for both underfilled and overflowing states: designed but needed more specificity
- `NavigationStack` + top blur: met
- reusable `ScrollView` component: met

Claude's main feedback:
- the underfill-to-overflow animation path needed a concrete mechanism, not just intent
- total content height should not be treated as globally reliable with `LazyVStack`
- pixel-precise prepend and self-sizing correction should plan on a private `UIScrollView` offset bridge
- the viewport mode state machine needed explicit transitions
- Phase 0 needed quantitative exit criteria, especially for append animation and threshold crossing

Changes incorporated into `ai/review-v2.txt` after review:
- added an explicit underfill animation strategy: animate filler shrink and row insertion in one transaction
- clarified that total content height is only used while underfilled; overflow mode relies on anchors and visible geometry
- changed the internal `UIScrollView` bridge from optional fallback to planned Phase 3 infrastructure
- added a mode transition list for `ViewportMode`
- moved underfill-to-overflow animation proof earlier and tightened Phase 0 exit criteria

Remaining risk after the revised spec:
- the plan now matches the stated requirements, but the hardest behaviors still need to be proven in Phase 0 and Phase 3
